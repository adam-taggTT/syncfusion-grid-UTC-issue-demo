# Syncfusion Grid UTC Issue Demo

This repository is a small demo created for a Syncfusion support ticket about a grid UTC/date issue.

It exists to reproduce the behavior in a minimal Angular app and is not intended to be published as a reusable package.

## Private status

The project is already marked `"private": true` in `package.json` to prevent accidental package publication.
